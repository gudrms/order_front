{
    "name": "toss-pos-plugin",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "zip": "vite build && cd dist && tar -cf ../plugin.zip .",
        "test": "vitest run"
    },
    "devDependencies": {
        "@tossplace/pos-plugin-sdk": "^0.0.16",
        "typescript": "^5.9.3",
        "vite": "^6.3.2",
        "vitest": "^4.0.16"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.89.0"
    }
}
