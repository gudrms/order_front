# ============================================
# 개발 환경 전용 변수 (npm run dev)
# ============================================

# API 서버 설정
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# Mock 데이터 설정
# NEXT_PUBLIC_STORE_ID: Mock 데이터에서 사용할 매장 ID
NEXT_PUBLIC_STORE_ID=store-default-1
# NEXT_PUBLIC_USE_MOCK: true일 경우 실제 API 대신 Mock 데이터를 사용
NEXT_PUBLIC_USE_MOCK=true

# 기본 매장/지점 설정 (URL에 없을 경우 사용)
NEXT_PUBLIC_DEFAULT_STORE_TYPE=tacomolly
NEXT_PUBLIC_DEFAULT_BRANCH_ID=gimpo

# 개발 도구 설정
NEXT_PUBLIC_ENABLE_QUERY_DEVTOOLS=true
NEXT_PUBLIC_LOG_LEVEL=debug

# ============================================
# 개인별 오버라이드 안내
# ============================================
# .env.development.local 파일을 생성하여 위 값들을 덮어쓸 수 있습니다.
