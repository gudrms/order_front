{
  "buildCommand": "cd ../.. && pnpm install && pnpm turbo build --filter=brand-website",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
