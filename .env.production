# ============================================
# 프로덕션 환경 전용 변수
# ============================================
# 이 파일은 Git에 커밋됩니다 (팀 공유).
# npm run build 실행 시 자동으로 로드됩니다.

# ⚠️ 주의: 실제 배포 시 서버에서 환경 변수를 덮어써야 합니다.
# 이 파일의 값은 기본값이며, 실제 도메인으로 변경해야 합니다.

# ============================================
# API 서버 설정 (프로덕션)
# ============================================

# REST API 기본 URL (실제 도메인으로 변경 필요)
# 예: https://api.tableorder.com/api/v1
NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api/v1

# WebSocket (STOMP) 연결 주소 (WSS - HTTPS 필수)
# 예: wss://api.tableorder.com/ws-stomp
NEXT_PUBLIC_WS_URL=wss://api.yourdomain.com/ws-stomp

# ============================================
# Node 환경
# ============================================
NODE_ENV=production

# ============================================
# 프로덕션 최적화
# ============================================

# TanStack Query DevTools 비활성화
NEXT_PUBLIC_ENABLE_QUERY_DEVTOOLS=false

# 로그 레벨 (에러만 출력)
NEXT_PUBLIC_LOG_LEVEL=error

# ============================================
# 배포 서버 설정 방법
# ============================================
# 1. NCP 서버에서 .env.production.local 파일 생성
# 2. 실제 도메인으로 환경 변수 설정:
#    NEXT_PUBLIC_API_URL=https://api.실제도메인.com/api/v1
#    NEXT_PUBLIC_WS_URL=wss://api.실제도메인.com/ws-stomp
# 3. 또는 서버 환경 변수로 직접 설정 (권장)
