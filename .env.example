# ============================================
# 환경 변수 설정 예시
# ============================================
# 이 파일은 환경 변수 설정 참고용 템플릿입니다.

# ============================================
# 🚀 빠른 시작
# ============================================
# 
# 1. 개발 환경: 별도 설정 불필요
#    → .env.development 파일이 자동 적용됨
#    → npm run dev 실행하면 됩니다.
#
# 2. 개인별 API 서버 주소가 다른 경우:
#    → .env.development.local 파일 생성
#    → NEXT_PUBLIC_API_URL=http://192.168.0.100:8080/api/v1
#
# 3. 프로덕션 배포:
#    → 서버에서 환경 변수 직접 설정 (권장)
#    → 또는 .env.production.local 파일 생성

# ============================================
# 공통 설정 (.env)
# ============================================

NEXT_PUBLIC_APP_NAME=Table Order System
NEXT_PUBLIC_APP_VERSION=0.1.0

# ============================================
# 개발 환경 (.env.development)
# ============================================

# NestJS Backend
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# 매장 ID (UUID)
NEXT_PUBLIC_STORE_ID=your-store-uuid

# 개발 도구
NODE_ENV=development
NEXT_PUBLIC_ENABLE_QUERY_DEVTOOLS=true
NEXT_PUBLIC_LOG_LEVEL=debug

# ============================================
# 프로덕션 환경 (.env.production)
# ============================================

# NestJS Backend (Vercel Serverless)
# NEXT_PUBLIC_API_URL=https://your-domain.vercel.app/api/v1

# Supabase
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# 매장 ID (UUID)
# NEXT_PUBLIC_STORE_ID=your-store-uuid

# 프로덕션 설정
# NODE_ENV=production
# NEXT_PUBLIC_ENABLE_QUERY_DEVTOOLS=false
# NEXT_PUBLIC_LOG_LEVEL=error

# ============================================
# 파일 구조
# ============================================
#
# .env                    # 공통 설정 (Git O)
# .env.development        # 개발 설정 (Git O)
# .env.production         # 프로덕션 설정 (Git O)
# .env.development.local  # 개발 개인 오버라이드 (Git X)
# .env.production.local   # 프로덕션 개인 오버라이드 (Git X)
# .env.example            # 이 파일 - 템플릿 (Git O)
